# Provisioned from a file, not clicked together in the UI and lost the
# next time the container is recreated (docs/observability-design.md).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
